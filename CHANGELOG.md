# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

## [v1.2.0] - 2026-09-08

### 新增

- **托盘自身更新检查**：启动后后台查询 GitHub Release 最新版本（`api.github.com/repos/3142698233/dsh-launcher/releases/latest`），发现新版本时气泡提示，点击提示直接打开 GitHub Release 下载页；网络不可用或无新版本时静默，不打扰使用
- 强制启用 TLS 1.2（GitHub API 要求；.NET 4.0 默认仅 TLS 1.0）

## [v1.1.0] - 2026-09-08

首个公开发布版本（Windows 桌面托盘启动器）。此前为本地开发/脚本阶段，未单独发布。

### 新增

- **原生托盘程序 `DshTray.exe`**：原生编译 WinForms，后台常驻运行 `dsh web`，平时无任何窗口，仅有任务栏右下角托盘图标（绿 = 运行中 / 灰 = 已停止）
- **两阶段启动架构**：阶段 A 版本检查/更新，阶段 B 直接启动服务器，两者完全解耦
- **真实版本对比**：启动时对比本地 dsh 版本与 npm 最新版（`npm view`，30 秒超时），有新版才执行 `npm install -g @deepseek-ai/dsh@最新版`（180 秒超时），结果记录到 `logs\dsh-update.log`
- **服务器定位**：优先使用全局 npm 安装的 dsh，其次 npx 缓存；不联网、不经 npx，启动更快更可靠
- **`--no-open` 启动参数**：服务器就绪后不再重复弹出浏览器
- **Token 自动附带**：托盘"打开界面"与双击图标自动携带 dsh 登录 token（`GetUiUrl`），解决 `dsh web authentication required` 提示
- **内置版本号**：exe 属性 → 详细信息可见 `1.1.0.0`
- **开机自启**：写入当前用户注册表 `Run` 键，托盘可一键开关；旧版自启项自动迁移指向新 exe
- **异常兜底**：启动失败弹窗显示日志末尾；服务器崩溃气泡通知、图标变灰、可一键重启
- **完整日志**：服务器输出、更新记录、操作日志全部落盘 `logs\`（含实际执行的命令脚本，可复核）

### 修复

- **根治"有更新时无法正常启动"**：更新与启动解耦，即使网络差、更新失败，也继续用现有版本启动
- **修复浏览器提示 authentication required**：打开界面自动附带随机登录 token
- **修复 npx 缓存文件锁/网络抖动导致的启动慢或失败**：改为直接调用已安装的 dsh 程序

### 变更

- 以 `.exe` 托盘程序取代早期 PowerShell 脚本版（开机自启项自动迁移，无需手动处理）
- 项目结构清理：与运行无关的历史调试文件移入 `_archive/`（git 忽略，不影响程序）

### 其他

- 首个公开版本，采用 [MIT](LICENSE) 许可证
- 附带完整 README（功能、两阶段原理、托盘菜单、FAQ）
